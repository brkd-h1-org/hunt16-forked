pull_request_rules:
  - name: merge when labeled go
    conditions:
      - label = go
    actions:
      merge:
        method: merge
  - name: delete head on close
    conditions:
      - closed
      - -merged
    actions:
      delete_head_branch:
        force: true
